# Fixture rubric

Apply the same four criteria to every build: `theme_fit`, `creativity`, `effort`, and
`detail`. Give each criterion an integer score from 1 to 10.

Always name one genuine strength. Never mock a build or use sarcasm about effort. Explain
your reasoning before the scores and finish with exactly two kind, useful sentences for the
builder.
